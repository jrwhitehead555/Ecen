import json

data = {}
data['students'] = []
data['students'].append({
    'name': 'Justin Whitehead',
    'GitHubID': 'jrwhitehead555',
    'NetID': '927007151'
})

with open('identity.txt', 'w') as outfile:
    json.dump(data, outfile)
